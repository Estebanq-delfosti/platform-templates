apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Servicio HTTP en Rust con Axum
  tags:
    - rust
    - axum
    - kubernetes
  annotations:
    github.com/project-slug: ${{ values.repoOwner }}/${{ values.name }}
spec:
  type: service
  lifecycle: experimental
  owner: group:default/platform-team
