apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: Servicio HTTP en Node.js con Fastify + TypeScript
  tags:
    - nodejs
    - typescript
    - fastify
    - kubernetes
  annotations:
    github.com/project-slug: ${{ values.repoOwner }}/${{ values.name }}
spec:
  type: service
  lifecycle: experimental
  owner: group:default/platform-team
