# ${{ values.name }}

${{ values.description }}

## Desarrollo

```bash
just run          # servidor local
just test         # tests con race detector
just build        # compilar binario
just docker-build # imagen Docker
```

## Stack

Go · chi · slog · Docker · Kubernetes
